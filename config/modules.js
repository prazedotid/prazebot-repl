const MODULES = {
    CHALK: require('chalk'),
    DEEPEXTEND: require('deep-extend'),
    MOMENT: require('moment'),
    MOMENT_DURATION_FORMAT: require('moment-duration-format'),
    OS: require('os'),
    PATH: require('path'),
    TABLE: require('table'),
    VORPAL: require('vorpal')()
}

module.exports = MODULES